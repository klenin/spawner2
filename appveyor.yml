branches:
  only:
    - /^v(\d+\.)+\d+$/
    - master

os: Visual Studio 2015

install:
  - appveyor DownloadFile https://win.rustup.rs/ -FileName rustup-init.exe
  - rustup-init -yv --default-toolchain stable --default-host x86_64-pc-windows-msvc
  - set PATH=%PATH%;%USERPROFILE%\.cargo\bin
  - rustc -vV
  - cargo -vV

build: false

cache:
  - '%USERPROFILE%\.cargo'
  - target

test_script:
  - cargo build
  - cargo test

after_build:
  - 7z a win32.zip %APPVEYOR_BUILD_FOLDER%\target\debug\sp.exe

artifacts:
  - path: win32.zip
    name: sp

deploy:
  provider: GitHub
  auth_token: $(GITHUB_AUTH_TOKEN)
  artifact: win32.zip
  force_update: true