pub struct Spawner {
    // todo
}

impl Spawner {
    pub fn new() -> Spawner {
        Spawner {
            // todo
        }
    }

    // pub fn run(&mut self, sess: &Session) {
    //     for exec in &sess.execs {
    //         self.run_exec(exec);
    //     }
    // }
}
